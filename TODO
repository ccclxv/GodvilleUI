VoiceImprover.js рефакторим LootImprover

доделать логгер Alls_HP - что-то не получается. Всем записям присваевается один 
и тот же класс. что-то с циклом намудрил...
сделать префикс для всех id/классов



багофиксы 
- в чате ctrl + " " теперь работает и в хроме,

ГОТОВО:
improvements.js
script.js
about.js - не трогаю
DungeonImprover.js - не трогаю
phrases и words - пока не трогаю
InterfaceImprover.js - не трогаю

-logger, informer, VoiceImprover.js, utils,
options-page, options - не трогаю 3 этап 

кусок кода, для логгера в режиме дуэли
		GroupHP: function(flag) {		
			var seq = 0;
			var $box = flag ? $('#alls .opp_h') : $('#opps .opp_h');
			var GroupCount =	$box.length;
			if (GroupCount > 0)
			{
				for (var i = 0; i < GroupCount; i++) {
					if (parseInt($box[i].textContent)) seq += parseInt($box[i].textContent);					
				}
			}
			return seq; 
		},	
=================================== Этап 2: Базовые объекты =============================
 + ui_data
 + "script.js" - диспетчер и инициализация
 
 - ui_storage - убрать clearStorage или привести в порядок
 - ui_utils - раскидать 
 - "informer.js" - информер
 - "timeout_bar.js" - это бы преобразовать в отдельный модуль, который запрещает кнопку 
"изречь"(хм. а её же voiceImprover должен запрещать...) и выдаёт эту полоску

"phrases.js" - а это авторские константы
"words.js" - а это то, что обрабатывает эти константы

Пока засунул обновления информера в монитор. Перенести в информер полную слежку пока не получится:
лут импрувер дает очень много обновлений, которые завязаны на его логику. Может оно и не надо?


